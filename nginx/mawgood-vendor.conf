# =============================================================================
# Nginx — Vendor Panel (SPA)
# /etc/nginx/sites-available/mawgood-vendor
#
# Same /api proxy strategy as admin panel.
# =============================================================================

upstream medusa_backend_vendor {
    server 127.0.0.1:9000;
    keepalive 16;
}

server {
    listen 80;
    listen [::]:80;
    server_name vendor.mawgood.cloud;

    # return 301 https://$host$request_uri;

    # ── /api proxy ────────────────────────────────────────────────────────────
    location /api/ {
        proxy_pass         http://medusa_backend_vendor/;
        proxy_http_version 1.1;

        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache_bypass  $http_upgrade;
        proxy_read_timeout  120s;
        proxy_connect_timeout 30s;

        client_max_body_size 50M;
    }

    # ── SPA static files ─────────────────────────────────────────────────────
    location / {
        proxy_pass         http://127.0.0.1:5174;
        proxy_http_version 1.1;

        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache_bypass $http_upgrade;

        proxy_intercept_errors on;
        error_page 404 = @spa_fallback;
    }

    location @spa_fallback {
        proxy_pass http://127.0.0.1:5174;
    }
}

# HTTPS — uncomment after: sudo certbot --nginx -d vendor.mawgood.cloud
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name vendor.mawgood.cloud;
#     ssl_certificate     /etc/letsencrypt/live/vendor.mawgood.cloud/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/vendor.mawgood.cloud/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     location /api/ {
#         proxy_pass http://medusa_backend_vendor/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#         client_max_body_size 50M;
#     }
#     location / {
#         proxy_pass http://127.0.0.1:5174;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_intercept_errors on;
#         error_page 404 = @spa_fallback;
#     }
#     location @spa_fallback { proxy_pass http://127.0.0.1:5174; }
# }
