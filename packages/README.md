# Shared Packages

This directory is reserved for shared packages used across the monorepo.

## Planned packages

- `packages/ui` — Shared UI components
- `packages/types` — Shared TypeScript types
- `packages/config` — Shared configuration (ESLint, Prettier, etc.)
